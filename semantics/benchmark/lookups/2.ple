Integer Lookup tick;
Command pprint(...);

ACTION : {
    Integer count = -1;
    Exit Lookup(tick,3) > 3;
    {
        Repeat true;
        Concurrence {
            count = Lookup(tick);
            pprint("Iteration", count);
        }
    }
    pprint("This should never be printed");
}
