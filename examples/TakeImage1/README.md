This directory contains PLEXIL plan TakeImage1 and its associated
model-checking files.  See README in parent directory for more info.

Expected behavior (currently has bugs):

When loading `load0.maude` followed by `check.maude`, Maude hangs.

When loading `load1.maude` followed by `check.maude`, Maude hangs.
